from setuptools import setup

setup(
   name='witExtractor',
   version='0.1',
   description='A useful module',
   author='Foo',
   author_email='foomail@foo.com',
   packages=['witExtractor'],  #same as name
)
